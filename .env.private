# Production-private build (`npm run build:private`, loaded via vite --mode private): renders only
# the coming-soon page so the public URL isn't a freely-usable app before web billing exists.
# Plain `npm run build` (no flag) keeps the full app for the preview/testing deploy.
VITE_PRIVATE=true
