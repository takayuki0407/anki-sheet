# Public build (`npm run build:public`, via vite --mode public): renders the FULL app. Used for the
# preview/testing deploy and, eventually, the real public launch. The default `npm run build` is
# private (coming-soon) so production stays non-public unless you explicitly opt in here.
VITE_PUBLIC=true
